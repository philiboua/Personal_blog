.pageBg
  = render 'shared/header'
  .titlePage
    %h4.text-center
      = markdown(@post.title)
      

  .col-md-8.col-centered
    %h2.title-article
      
    .p-article{style: 'font-size: 20px;'}
      = markdown(@post.body)
    .comments
      = render 'post_comments'
   