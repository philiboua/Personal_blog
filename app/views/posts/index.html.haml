.col-md-8.col-centered
  - if @posts.present?
    - @posts.each do |post| 
      .article-preview
        %h3.title-article-preview
          = link_to "#{post.title}", post_path(post.slug)
        %p
          = markdown post.body.first(210) + "..."
        = link_to 'read more',post_path(post.slug)
    .post-pagination.text-center
      = will_paginate @posts, renderer: BootstrapPagination::Rails